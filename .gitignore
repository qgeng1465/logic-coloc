__pycache__/
*.py[cod]
.pytest_cache/
.claude/settings.local.json
.env
.env.*
!.env.example
.venv/
venv/
*.log
.DS_Store
Thumbs.db
# Word/WPS 打开文档时留的锁文件（~$xxx.docx），不是内容
~$*

# Local H5 note data and user uploads
data/notes.json
data/cards.json
data/review_records.json
uploads/

# 账号：凭证索引（含密码 hash）、每账号的目录（含资料）、token 签名密钥。
# 这三样都是本机跑出来的运行时状态，绝不能进版本库。
# data/corpus/example.jsonl 是语料库，必须留在库里，不要写成整目录忽略。
data/users.json
data/users/
data/.secret_key

# CLI 演示缓存与出图（precompute / make_figures 生成）——本机产物，不进库。
# ⚠️ 这两条必须带前导斜杠（锚定到仓库根）：写成 assets/ 会把前端的 web/assets/
# 站点图标一起忽略掉，那目录是要进镜像的。
/data/demo_cache.json
/assets/
