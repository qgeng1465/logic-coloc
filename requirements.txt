# Logic-Coloc 运行依赖（版本为本地已验证可跑通的版本）
# 安装：pip install -r requirements.txt

fastapi==0.138.1
uvicorn[standard]==0.49.0
pydantic==2.10.3
langgraph==1.2.9
numpy==2.2.6
scipy==1.18.0
requests==2.32.3

# ⚠️ 必须有，别删。routes.py 里三个路由（/api/user/avatar、/api/ocr、/api/upload）
# 用了 `UploadFile = File(...)`，而 fastapi 的 File/Form 支持是**可选依赖**，
# 单独一条 `fastapi==…` 不会把它装进来。
# 缺了不是「上传不能用」这么轻 —— 它在**注册路由时**就抛 RuntimeError
# （fastapi/dependencies/utils.py: ensure_multipart_is_installed），
# 也就是**应用启动即崩，整站打不开**。
# 本地之所以看不出来：开发机上被 streamlit 之类无关的包顺手装过（fastapi[standard]
# 也会带），所以 import 一直是好的；一进干净环境（容器、队友的新机器）就暴露。
python-multipart==0.0.32

# 图片 OCR（/api/ocr 接口）。会拖入 onnxruntime，镜像体积和冷启动时间明显变大。
# 若演示不需要「上传截图识字」这个功能，删掉这一行可以大幅瘦身。
rapidocr_onnxruntime==1.2.3

# PDF 文字提取（笔记复盘要读 PDF 附件，见 api/attachment_text.py）。纯 Python、
# 无 native 依赖，体积和冷启动影响可忽略。注意：只能取 PDF 的文字层，扫描件取不到。
pypdf==6.18.1

# 说明：
# - matplotlib 只在 make_figures.py / plots.py 里用（离线出图），线上不需要，故意不列。
# - pytest 只在跑测试时需要，线上不需要，故意不列。
# - Python 版本锁定 3.13：镜像用 python:3.13-slim，与本地开发环境完全一致。
#   曾经写过「建议 3.11」，那是错的 —— scipy==1.18.0 没有 3.11 能用的包，
#   在 3.11 容器里 pip install 直接失败（No matching distribution）。
#   整个依赖树在 cp313 / linux x86_64 上全部有预编译 wheel，无需现场编译。
